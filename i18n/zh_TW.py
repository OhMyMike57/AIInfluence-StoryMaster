"""Traditional Chinese (zh_TW) — the default/fallback language.

Because tr() returns the key itself when no mapping is found, and all UI
strings are already written in zh_TW, this dictionary can stay empty.
It exists as an explicit declaration that zh_TW is a supported locale.
"""

STRINGS: dict = {}
